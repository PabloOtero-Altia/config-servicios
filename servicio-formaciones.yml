server:
  port: 8081
spring: 
  mvc:
    pathmatch: 
      matching-strategy: ant-path-matcher
  application:
    name: servicio-formaciones
eureka:
  instance:
    instance-id: ${random.value}:${spring.application.name}
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
